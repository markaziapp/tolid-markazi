{
  "name": "tolid-markazi-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute tolid-markazi-db --file=../schema/schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
